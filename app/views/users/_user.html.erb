
  <div class= "col-lg-3" >
    <div class="row">
      <h2>User info</h2>
      <tb><%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg", size:'40x40' %></tb>
      <table class="table">
        <tbody><tr></tr>
          <tr>
            <th>name</th>
            <th><%= user.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
            <th><%= user.introduction %></th>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

  <div class="row">
    <!--<a class="btn btn-outline-secondary btn-block fas fa-user-cog edit_user_27706" href="/users/27706/edit"></a>-->
    <%= link_to '', edit_user_path(user.id), class: 'btn btn-outline-secondary btn-block fas fa-user-cog'%>
  </div>
